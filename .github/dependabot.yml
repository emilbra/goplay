# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # MAKE SURE TO UPDATE THIS TO BE THE CORRECT PACKAGE ECOSYSTEM, referencing https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file#package-ecosystem
  # as it is now, it will fail because it is not a valid package ecosystem, if you use this for terraform, it will work
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: ""
